/**
 * Player Class - Representa um jogador (Humano ou IA).
  */
  export class Player {
      constructor(symbol, name) {
              this.symbol = symbol;
                      this.name = name;
                              this.score = 0;
                                      this.isAI = false;
                                          }
                                          }
